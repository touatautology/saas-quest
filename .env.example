# This is an example of your .env file format, which pnpm db:setup will create.
# Note: this must be .env, not .env.local, without further configuration changes.

# Database (PostgreSQL with pgvector extension)
POSTGRES_URL=postgresql://postgres:postgres@localhost:5450/saas_quest

# Authentication secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your-auth-secret-here

# Base URL
BASE_URL=http://localhost:3000

# Stripe (for SaaS Quest billing)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Encryption key for storing API keys securely (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=your-32-byte-base64-encoded-key

# Google Analytics 4 (optional - for admin analytics)
# GA4_PROPERTY_ID=
# GA4_CLIENT_EMAIL=
# GA4_PRIVATE_KEY=